
git init
git add .
git commit -m "setup"

git remote add origin 

git branch -M main
git push -u origin main


### if there is an error saying you need to initialise username and email ###

git config --global user.name "MinelDesilva1010"
git config --global user.email "minelshelly@gmail.com "
